import EQUIPMENTS_DETAIL from '../../../base/data/equipments-detail'

const INITIAL_STATE = EQUIPMENTS_DETAIL

const equipmentReducer = (state = INITIAL_STATE, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default equipmentReducer